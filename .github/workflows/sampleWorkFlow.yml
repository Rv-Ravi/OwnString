name: Sample workflow

on:
  push:
    branches:
      - master

env:
  value: Ravishankar

jobs:
  MySceondJob:
    name: mysecondJob
    uses: ./.github/workflows/workflowOnCall.yaml
    with:
      value1: Ravi
      value2: 26

  MyFirstJob:
    name: myfirstJob
    runs-on: ubuntu-22.04
    needs: [MySceondJob]
    steps:
      - name: checkout
        uses: actions/checkout@v4
        
      - name: step1
        run: |
          echo ${{ needs.MySceondJob.outputs.mainOut }} 
          echo ${{ needs.MySceondJob.outputs.mainAge }}

      
